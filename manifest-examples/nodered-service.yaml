apiVersion: v1
kind: Service
metadata:
  name: nodered-service
  labels:
    app: nodered
spec:
  type: NodePort
  selector:
    app: nodered
  ports:
    - port: 1880
      targetPort: 1880
      nodePort: 31880
